# Decisions for this non-interactive eval run (the skill's preflight gate reads this).
# Coalesce's node graph is already dimensional, so the target modeling mirrors it (kimball/star);
# 'layered' also works. The SCD2 dimension becomes a dbt snapshot.
target_modeling: star
data_warehouse: duckdb
packages_mode: external_hub
landing_spot: /app/project
